\documentclass{../lecture}

\title{Lecture 4}
\date{January 26, 2026}

\begin{document}

\maketitle

\begin{frame}{From Previous Lectures}
  \begin{itemize}
    \item Build shell scripts with predictable behavior in spite of:
      \begin{itemize}
        \item Working Directory
        \item Undeclared environment variables
        \item State of the filesystem
      \end{itemize}
    \item The kernel unpacks an initramfs, which is purely in memory. We mount
      our disk, then \texttt{switch\_root}.
  \end{itemize}
\end{frame}

\begin{frame}{Pointers and Strategies}
  \begin{itemize}
    \item Debugging things in this class can be incredibly difficult!
    \item Are your archives laid out correctly?
    \item Are all your set paths correct?
    \item Are your binaries not broken?
    \item Is your kernel not misconfigured (no networking, bad cmdline, ...)?
    \item Are your config files not misconfigured?
    \item Are your environment variables correct?
    \item Are your file permissions correct?
  \end{itemize}
  Always think:
  \begin{itemize}
    \item Where are the possible points of failure in system bring up?
    \item How could I try checking whether this is a point of failure?
    \item Is there something evil hiding in the logs? (When make or configure
      fails)
  \end{itemize}
\end{frame}

\begin{frame}{Note Grading Environment Permissions!}
  \begin{itemize}
    \item Your entire filesystem will be locked down in grading.
    \item \texttt{/workspace} will be read-only
    \item Only \texttt{\$DIST} will be readable.
    \item You should be doing out of tree builds.
    \item Your main build pipeline should not have interactive steps.
    \item There are scripts on the repo to help you test this! Consult the
      README for more details.
  \end{itemize}
\end{frame}

\begin{frame}{Final Note Re Integrity}
  Permitted:
  \begin{itemize}
    \item Discussing ideas, design choices, and debugging strategies
    \item Reading documentation, blog posts, and reference material
    \item Using AI tools to explain errors or suggest approaches
  \end{itemize}

  \textbf{However, the assignment must reflect your own reasoning.} Do not:
  \begin{itemize}
    \item copy another student's root filesystem, disk image, or VM
    \item reuse configuration files (\texttt{/etc/*}, init scripts, etc.) verbatim
    \item start from someone else's working system and modify it
  \end{itemize}
\end{frame}

\begin{frame}{This Lecture}
  \begin{itemize}
    \item We close the story on userspace and filesystems.
    \item Part 1: User programs, root filesystem, and configuration
    \item Part 2: Partitions and Filesystems
    \item \textbf{Reminder: Do Attendance!}
  \end{itemize}
\end{frame}

\begin{frame}{Initramfs Rootfs Distinction}
  The Initramfs:
  \begin{itemize}
    \item Gets archived as a cpio.gz file and embedded into the kernel
    \item Unpacked by the kernel at startup and backed by RAM
    \item Only real task is to switch to root.
  \end{itemize}
  The real Rootfs:
  \begin{itemize}
    \item Backed up by the disk
    \item Contains the actual relevant files for your operating system
    \item What you will be focusing on in the next few labs
  \end{itemize}
\end{frame}

\begin{frame}{The FHS (Again)}
  The root filesystem needs to follow the filesystem hierarchy standard even
  more strictly than the initramfs.
  \begin{block}{You need these:}
    \begin{itemize}
      \item \texttt{/usr/bin}, \texttt{/usr/share}, \texttt{/usr/include}
      \item \texttt{/lib}, \texttt{/bin}
      \item \texttt{/etc}
      \item \texttt{/run}, \texttt{/proc}, \texttt{/sys}, \texttt{/dev}
    \end{itemize}
  \end{block}
  In effect, a near-superset of your initramfs tree.
\end{frame}

\begin{frame}{/etc}
  \begin{itemize}
    \item Important system configuration files will be put here.
    \item PID 1 configuration
    \item daemon configuration
    \item filesystem mount points (fstab)
    \item Package manager config files (not in this class)
    \item ... (more files in complex systems)
  \end{itemize}
  We recommend you inject these into your user filesystem once figured out!
\end{frame}

\begin{frame}{PID 1}
  Usually, PID 1 will:
  \begin{itemize}
    \item Reap orphaned zombies (essential!)
    \item Mounts filesystems
    \item Spin up daemons
    \item Maybe other functionality too
    \item If PID 1 Fails, \textbf{The Kernel will panic!}
  \end{itemize}
  \begin{exampleblock}{Examples of Init}
    \begin{itemize}
      \item Busybox init (We use this for simplicity)
      \item Systemd (popular but controversial!)
      \item OpenRC, runit
    \end{itemize}
  \end{exampleblock}
\end{frame}

\begin{frame}{Configuring PID 1}
  \begin{itemize}
    \item Taken from conventions of System V UNIX (released 1983)
    \item This is \textit{one} of many ways init systems can be configured
    \item \texttt{/etc/inittab} describes \emph{what} should run and \emph{when}
    \item Entries can be one-shot, respawned, or tied to boot stages (startup,
      shutdown, \dots)
    \item Long-running services must be restarted if they die
  \end{itemize}
  \begin{exampleblock}{Startup scripts (See next slide)}
    \begin{itemize}
      \item Typically a script like \texttt{/etc/init.d/rcS}
      \item Responsible for mounting filesystems and starting daemons
      \item Invoked automatically by \texttt{init}
    \end{itemize}
  \end{exampleblock}
\end{frame}

\begin{frame}{Boot Levels}
  BusyBox init runs programs in phases.
  \begin{block}{Important boot stages}
    \begin{itemize}
      \item \texttt{sysinit} — run once, very early
        \begin{itemize}
          \item Mount \texttt{/proc}, \texttt{/sys}, \texttt{/dev}
          \item Prepare runtime directories
        \end{itemize}
      \item \texttt{respawn} — long-running services
        \begin{itemize}
          \item \texttt{getty}, login shells
          \item Restarted automatically if they die
        \end{itemize}
      \item \texttt{shutdown} — cleanup operations on poweroff/reboot
    \end{itemize}
  \end{block}
  You should think about ordering very clearly.
\end{frame}

\begin{frame}{Configuring Users and Groups}
  \begin{itemize}
    \item The most basic way in which the operating system regulates access to
      privileged resources.
    \item User names and Group names are really aliases for integer UID's and GID's
    \item You only need to have \texttt{root} and \texttt{dhcpcd} (see handout
      for more info on dhcpcd)
    \item Root is all-powerful, generally avoid being root because you can break
      a lot of things!
  \end{itemize}
  \begin{block}{Configuration files (passwd and group read by musl)}
    \begin{itemize}
      \item \texttt{/etc/passwd}: Contains metadata about users
      \item \texttt{/etc/shadow}: Contains hashed passwords (needs very
        restrictive permissions)
      \item \texttt{/etc/group}: Contains metadata about groups
    \end{itemize}
  \end{block}
\end{frame}

\section{Important Userspace Daemons}

\begin{frame}{Dhcpcd}
  \begin{itemize}
    \item You basically need DHCP on modern networks for any kind of internet
      access
    \item The kernel will not do IP address configuration for you
    \item You can test working by \texttt{ping 1.1.1.1} (or some other raw ip
      address; DNS will be covered later)
    \item Dhcpcd requires its own user and group, along with a writable
      directory of its own! (See handout)
  \end{itemize}
\end{frame}

\begin{frame}{Other Essential Daemons (You must start them!)}
  \begin{block}{getty}
    \begin{itemize}
      \item Spawns login prompts on \texttt{/dev/tty*}
      \item One instance per terminal
      \item Reads \texttt{/etc/shadow} to allow access
    \end{itemize}
  \end{block}
  \begin{block}{eudev}
    \begin{itemize}
      \item Userspace device manager
      \item Creates device nodes under \texttt{/dev}
      \item Triggers driver loading and hotplug events
    \end{itemize}
  \end{block}
  \begin{block}{chrony}
    \begin{itemize}
      \item NTP client for wall-clock time
      \item Corrects clock drift after boot
      \item Required for TLS and sane logs
    \end{itemize}
  \end{block}
\end{frame}

\section{Filesystems}

\begin{frame}{What is a Filesystem?}
  \begin{itemize}
    \item A filesystem defines how bytes on disk are interpreted
    \item The filesystem interface is in the kernel (Linux is monolithic!)
    \item It maps names to data and metadata
    \item It defines persistence across reboots
  \end{itemize}
  \begin{block}{Responsibilities}
    \begin{itemize}
      \item File and directory naming
      \item Permissions and ownership
      \item Metadata (timestamps, size, links)
      \item Crash recovery and consistency guarantees
    \end{itemize}
  \end{block}
\end{frame}

\begin{frame}{Filesystems we use}
  \begin{block}{Ext4}
    \begin{itemize}
      \item Standard Linux filesystem
      \item Journaling (crash recovery), some papers about this!
      \item Allows for permissions, other complex metadata
    \end{itemize}
  \end{block}
  \begin{block}{VFat}
    \begin{itemize}
      \item Readable by EFI Firmware
      \item Much simpler, no permissions or even symlinks!
      \item We will put just the kernel in a special path in this filesystem.
    \end{itemize}
  \end{block}
  There are obviously other filesystems out there, but these are most standard.
\end{frame}

\begin{frame}{GPT}
  \begin{itemize}
    \item You want partitions to have different filesystems on the same disk.
    \item GUID Partition Table (replaces MBR)
    \item A standardized partition table format
    \item Unique identifiers for each partition
    \item Well-defined partition types (not just numbers)
  \end{itemize}
  \begin{block}{Regarding Firmware}
    \begin{itemize}
      \item Firmware reads metadata at the front of the disk (sectors 1-33)
      \item Special partition types signal boot-related data
      \item The EFI System Partition (ESP) is identified this way
      \item For legacy BIOS, sector 0 is just executed. (+ checking last
        two bytes are \texttt{0x55AA})
    \end{itemize}
  \end{block}
  You should consult the handout for further info!
\end{frame}

\begin{frame}{Before we keep moving}
  There are some toolchain constraints in the assignment!
  \begin{itemize}
    \item We will use filesystem debug tools and \texttt{sgdisk} to adjust
      metadata and partition layouts.
    \item LFS manuals will often get you to create a loopback device, which is
      impossible inside a docker container.
    \item If you attempt to copy files into filesystems via loopback devices,
      your build pipeline will fail.
  \end{itemize}
\end{frame}

\begin{frame}{Boot Path Summarized}
  (Cue blackboard)
  \begin{enumerate}
    \item Firmware starts
    \item Firmware figures out where ESP is from GPT metadata
    \item Launch bootloader (aka kernel) inside ESP (fixed path)
    \item Kernel unpacks initramfs
    \item initramfs \texttt{/init} mounts the real filesystem, then
      runs \texttt{switch\_root}
    \item \texttt{/sbin/init} in the disk takes over and launches programs.
    \item login screen (hopefully)
  \end{enumerate}
\end{frame}

\begin{frame}{Up Next}
  \begin{itemize}
    \item Topics beyond the lab: Graphical Environments, Dbus, Certificates,
      DNS, \dots
    \item Course Wrap-up, final lab hours
  \end{itemize}
\end{frame}

\end{document}
